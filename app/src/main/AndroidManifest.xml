<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="blue.arche.sample_1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA_EXTENDED" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HapticsOverLayActivity"
            android:label="@string/title_activity_haptics_over_lay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SurfaceViewActivity"
            android:label="@string/title_activity_surface_view"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".StoreActivity"
            android:label="@string/title_activity_store"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NearMeActivity"
            android:label="SeeGet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".catchoom.test.SplashScreenActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".catchoom.test.RecognitionFinderActivity"
            android:screenOrientation="portrait">



            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".catchoom.test.RecognitionSingleShotActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".catchoom.advancedexamples.ExtendedRecognitionActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".catchoom.test.LaunchersActivity" />
        <activity android:name=".catchoom.test.WebActivity" />
        <activity
            android:name=".catchoom.test.ScreenSlideActivity"
            android:screenOrientation="portrait" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.catchoom.test.PictureScreenShotActivity"
            android:label="@string/title_activity_picture_screen_shot"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScreenShotTestActivity"
            android:label="@string/title_activity_screen_shot_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PinchExampleActivity"
            android:label="@string/title_activity_pinch_example"
            android:theme="@style/AppTheme.NoActionBar">



        </activity>
    </application>

</manifest>